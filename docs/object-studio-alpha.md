# Object Studio Alpha

This increment adds a large engineering viewport, object transforms, primitive creation, surface materials, texture upload, camera presets, grid snapping, measurements, scene lighting, local persistence, and JSON export.

Advanced topology editing remains a later milestone.
